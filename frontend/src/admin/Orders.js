import React from "react";
import Base from "../core/Base";
const orders = () => {
    return (
        <Base>
            <h1 className="">Orders</h1>
        </Base>
    );
};
export default orders;
